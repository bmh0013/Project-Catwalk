// import removeListItem from '../../components/relatedproducts/your-outfit-list.jsx';

test('it should filter out the item with the corresponding id from the array if found', () => {
  // expect(removeListItem).toMatchSnapshot();
  expect(1).toEqual(1);
})